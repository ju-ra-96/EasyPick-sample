Sample frontend for phone recommender system

1. clone the repository
2. run "npm install"
3. run "npm start"

\*in case any external dependency doesn't get installed please run "npm install [TheNameOfTheDependency]"
